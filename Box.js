class Box extends BaseClass{
  constructor(x, y, width, height) {
    super(x, y, width, height);
    this.image = loadImage("sprites/wood1.png");
    
  }
 };

//super() - transfers all the properties from the constructor of the Base Class
